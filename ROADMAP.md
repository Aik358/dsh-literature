# dsh-literature 功能拓展路线图

对标 ChatPDF / SciSpace / Zotero / Readwise Reader，基于现有架构（内置文献库 + PDF 阅读器 + AI 对话注入 + 引用生成 + 全文获取）规划。

## 近期（架构已支持，改动小、收益直接）

### 阅读体验
- **阅读位置记忆**：退出阅读时记录页码/滚动位置，重进自动回到原处（store 已有条目级持久化，补一个 `readerProgress` 字段即可）
- **键盘快捷键**：←/→ 翻页、+/- 缩放、`/` 聚焦搜索、`Esc` 关闭浮条（viewer 已有键盘事件挂点）
- **夜读模式**：canvas 反色（`filter: invert(1) hue-rotate(180deg)`）+ 高亮混合模式适配，宿主切暗色时自动生效
- **页面缩略图侧栏**：快速跳页（pdf.js 已按页渲染，缩略图复用同一渲染管线低 scale 输出）

### 高亮与笔记
- **高亮颜色自选**：划词浮条加 4 色色板（替换当前的自动轮换）
- **高亮导出**：一键导出全部高亮+笔记为 Markdown（`页码 | 原文 | 笔记` 表），写论文时直接引用
- **笔记 Markdown 化**：笔记字段支持简单 markdown 渲染

### 文献库管理
- **标签与过滤**：条目打标签（复用 preferredTags 基建），列表按标签/状态过滤（全部 / 未读 / 已入库 / 失败重试）
- **排序**：标题 / 年份 / 添加时间
- **批量操作**：多选条目 → 批量导出 RIS/BibTeX/CSL-JSON（exporter 已支持格式，补多选 UI）

### 引用
- **BibTeX 输出**：cite.js 补 bibtex 格式（CSL 数据已齐，加一个序列化器）
- **引用菜单直达**：卡片右键直接出引用菜单

## 中期（需要宿主能力配合或较大开发）

- **AI 上下文连贯**：划词提问时自动附带"该条目的既有笔记/高亮"作为上下文，追问不用重复解释
- **多文档对比问答**：选 2-3 篇文献一起问（差异、方法对比）——依赖宿主 agent 的多附件能力
- **AI 自动整理**：入库时自动生成一句话摘要 + 建议标签（走现有 ai/ask 通道，结果写回条目）
- **新文订阅**：按关键词订阅 arXiv/PubMed 每日新文，宿主定时任务 + SSE 推送到面板
- **Zotero 双向同步**：现在的 Zotero 通道是单向导出；双向需处理冲突（本地 store 时间戳 vs Zotero version）
- **库备份/迁移**：一键导出全库 JSON（含 PDF 打包 zip）/ 导入恢复

## 长期

- **手写批注层**：pointer events 画笔（PDF 批注 ink annotation），触摸屏可用
- **扫描件 OCR**：tesseract.js 离线 OCR，解锁扫描版 PDF 的搜索与 AI 问答（当前明确提示"可能是扫描件"的边界即由此打通）
- **移动端适配**：面板布局在窄屏的 drawer 形态已有基础（@media 520px），跟随宿主 remote 能力走

## 原则

- 每个功能先服务"读文献 → 管文献 → 写论文"主链路，不做与主链路无关的堆料
- AI 能力一律走宿主对话注入（`agent.steer`），插件不自带 LLM 调用——保持零额外配置、零额外费用
- 遵守既定边界：全文获取仅限 OA/已授权来源，付费墙走登录/手动导入路径
