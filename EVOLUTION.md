# dsh-literature 进化方向

面向 0.2.8 之后的演进判断。与 `ROADMAP.md` 的分工：**ROADMAP 回答"做什么功能"，
本文回答"往哪儿长、为什么、以及现在还差什么"**。所有结论均对照当前代码，
不写没有实证支撑的设想。

---

## 一、当前坐标

插件已经跑通了完整闭环：**识别 → 解析 → 下载 → 存储 → 阅读 → 批注 → 引用 → 导出**，
且全程本地、零外部依赖（内置库模式）。

真正的差异化不在"又一个 Zotero 侧栏"，而在于两点：

1. **它长在对话里。** 文献是在和模型聊天时被发现的，上下文天然就在手边。
2. **AI 走宿主注入。** `src/node/ai.js` 不持有任何模型端点，把问题转成消息推回当前
   DSH 会话。零配置、零密钥、零额外费用。

这两点决定了后面的路该怎么走：**凡是放大"对话 × 文献"耦合度的方向都值得做，
凡是把它变成独立文献管理器的方向都该警惕**（那片战场 Zotero 已经赢了）。

---

## 二、结构性改进点（先补短板）

这几项不是新功能，是决定上面那条路能走多远的地基。按风险排序。

### 2.1 品牌一致性的最后一公里 —— tool 名仍带 Zotero

`src/node/tools.js` 注册的两个工具叫 `zotero_lookup` / `zotero_save`，
description 里也直接写"保存到本地 Zotero 库"。

工具名和描述是**模型可见面**——它们进入 function-calling 的工具清单，
在部分宿主 UI 上也会直接呈现给用户。这与既定的品牌策略（可见面用"文献库"、
技术/同步层保留 Zotero）是冲突的，属于换名时遗漏的一块。

改造成本低（`name` + `description` + 别名兼容），但**改名是一次性窗口**：
包一旦被广泛安装，工具名就成了事实接口。建议在 0.3.x 内处理。

### 2.2 单文件 JSON 存储的规模上限

`src/node/store/db.js` 用一个 JSON 文件承载全部条目、任务、批注、导入记录，
`load()` 后整体驻留内存，`flushTimer` 做防抖落盘。

- 条数千级以内完全够用，无需引数据库；
- 但**批注是持续增长的部分**：每篇论文几十条高亮是常态，几百篇就是上万条，
  每次保存都要整库序列化一遍；
- 风险形态不是"慢"，而是**写入放大 + 中断丢批注**（防抖窗口内崩溃会丢最近一批）。

建议的低成本解法（不必上 SQLite）：把 `annotations` 拆成独立文件分片
（按 item key 一个文件），主库只存索引。改动集中在 db.js 一处，对外 API 不变。

### 2.3 库内检索能力缺失

现状：外部元数据检索有（`metadata/search.js` 调 Crossref/OpenAlex），
但**库内查找基本靠前端过滤**。条目一多，"我上次那篇讲 X 的论文在哪"
只能靠标题排序人肉翻。

这是从"能存"到"能用"的分水岭。最低成本方案：在 node 侧加一个倒排索引
（标题 + 作者 + 摘要 + 批注文本），内存构建、库变更时增量更新，
不需要引入任何依赖。

### 2.4 AI 链路是单向的：模型只会"推"，不会"拉"

目前模型能调用 `lookup`（识别文献）和 `save`（保存），但**无法读取用户
已有的库**。于是对话里会出现这种割裂：

> 用户："我之前存过一篇关于 SSVEP 的论文，作者是谁来着？"
> 模型：不知道。（它没有工具可以查）

这是当前最大的能力缺口，也是**最大的机会**——见第三节。

### 2.5 测试形态：回归强、端到端弱

现有 `scripts/` 覆盖扎实（构建产物、settings 渲染、i18n、host 集成），
且我这次给 i18n 补了"源码零硬编码中文"这类结构性断言。

但缺一块：**没有真实 PDF 的端到端路径**（下载 → 文本抽取 → 高亮 → 导出）。
每次改动阅读器都靠人工验证。建议加一个 fixtures 级 smoke：
用一个体积很小的固定 PDF 跑完整条链。

### 2.6 已知边界：扫描件是 AI 问答的死角

`pdf-text.js` 依赖 PDF 内嵌文本层。扫描件（无文本层）抽不出内容，
于是"对全文提问""总结全文"对它无效。目前是静默降质。

短期：抽不到文本时**明确提示"扫描件暂不支持全文问答"**，而不是给一个空答案。
长期：OCR 是唯一解，但会显著增大体积，应做成可选依赖或外挂。

---

## 三、创新点（差异化，按杠杆排序）

### 3.1 【最高杠杆】让模型会"查库"——从注入到双向

新增一组只读工具，把库暴露给模型：

| 工具 | 作用 |
|---|---|
| `search_library` | 按关键词/作者/年份检索已存文献，返回条目摘要 |
| `get_item_detail` | 取某条的完整元数据 + 批注 + 笔记 |
| `list_recent` | 最近添加 / 最近阅读 |

一旦打通，对话体验会从"文献侧栏"升级为**模型的长期记忆外挂**：

> "我库里有哪些讲 BCI 的？" → 模型列出 → "把 2023 年之后那篇的摘要给我"
> → "它在方法上和 ChatPDF 那类工具比有什么不同" → 基于真实批注回答。

**这是竞品最难复制的部分**：ChatPDF / SciSpace 是"上传一篇聊一篇"，
没有跨会话的库；Zotero 有库但没有模型。插件天生同时拥有两者。

风险与约束：只读工具要严格限制返回体量（避免把整库塞进上下文），
每条返回控制在几百字以内，并带 key 供按需取详情。

### 3.2 【高价值】引用真实性校验 —— 反幻觉

模型生成参考文献时会编造 DOI 和标题，这是学术场景最痛的问题之一。

提供 `verify_citation` 工具：输入一条引用文本 → 用 Crossref / OpenAlex
校验是否存在、返回权威元数据 → **把真实结果回灌到对话**。

对用户的价值不是"又一个查重工具"，而是：**当模型写错时，插件当场纠正它**。
这个功能一旦做好，是极强的口碑点，也非常契合"学术"定位。

实现要点：元数据解析链路已存在（`metadata/` 一套），主要工作是做
"引用文本 → 标识符"的模糊匹配 + 置信度阈值 + 不确定时如实说不确定。

### 3.3 阅读轨迹 → 研究脉络

现在高亮和笔记只是"存下来了"。它们是用户注意力留下的痕迹，价值远未被挖掘：

- **研究周报**：本周读了哪些、批注集中在哪几篇、哪些主题反复出现；
- **概念脉络**：跨文档的高亮聚合，找出用户反复标注的关键词，形成个人"术语表"；
- **回看提醒**：读过但没做完的（有高亮无笔记、读了几页就停）。

这把插件从"被动容器"变成"主动研究助理"，且完全基于已有数据，
不需要新数据源。

### 3.4 跨文档综述

选定 N 篇 → 抽取各自的关键段落（摘要 + 结论 + 用户高亮）→ 拼成结构化提示
→ 注入对话，让模型生成对比表 / 异同分析 / 时间线。

技术上不难（PDF 文本抽取、AI 注入都已具备），难在**上下文预算**：
`ai.js` 现在 `MAX_CONTEXT_CHARS = 60000`，多文档时要做智能截断与分配策略，
并明确告诉用户"基于哪些部分生成的"。

### 3.5 沉淀式文献卡片

每篇论文自动生成并允许用户编辑一张结构化卡片：
**一句话结论 / 方法 / 主要发现 / 局限 / 与我研究的关系**。

积累起来就是一份个人知识资产，可导出为 Markdown 笔记库或表格。
与 3.3 的脉络分析结合后，能形成真正的"第二大脑"雏形。

### 3.6 从写作到引用（反向链路）

现有链路是"读文献 → 生成引用"。反向也值得做：
**用户在对话里写段落时，让模型插入引用**——模型调用
`search_library` 找到支撑文献 → 插入格式正确的文内引用 →
自动生成对应参考文献条目。

cite.js 已有 APA/GB-T/MLA/Chicago/BibTeX，基础是现成的。

---

## 四、演进路径

### 阶段 A · 稳固（0.3.x）——把地基补齐

- 工具名去 Zotero 化 + 别名兼容
- 批注存储分片，消除写入放大
- 库内全文检索（倒排索引）
- 扫描件无文本时明确提示，而非静默降质
- 补一个真实 PDF 的端到端 smoke

**验收**：上千条目、上万批注下保存不卡；库内能秒级搜到内容。

### 阶段 B · 让模型会用库（0.4.x）——建立壁垒

- `search_library` / `get_item_detail` / `list_recent`（只读，严格控量）
- `verify_citation` 校验与回灌
- 引用菜单与库检索打通（右键 → "在对话中引用这篇"）

**验收**：模型能回答"我库里有没有讲 X 的论文"，且不会把整库塞进上下文。

### 阶段 C · 从管理器到研究助理（0.5+）

- 研究周报 / 概念脉络 / 回看提醒
- 跨文档综述（含上下文预算策略）
- 文献卡片沉淀与导出
- 写作侧反向引用插入

**验收**：用户每周能从插件里得到一条"我最近读了什么、关注什么"的总结。

---

## 五、明确不做的事

边界比路线更重要。以下方向即使看起来诱人也不做：

- **不自建模型端点。** AI 一律走宿主对话注入。这是零配置优势的来源，
  一旦引入密钥管理，产品性质就变了。
- **不碰付费墙。** 全文获取只走 OA / 用户自有权限（机构镜像、已购 PDF 导入）。
  遇到付费内容，打开出版方页面让用户自行登录，不做任何绕过。
- **不做通用知识库。** 网页剪藏、笔记管理、待办——都不做。
  偏离"学术文献"这条主线，就会在错误的战场上和成熟的竞品消耗。
- **不追求库规模指标。** 服务的是"读 → 管 → 写"主链路，
  不是做一个能装十万条的仓库。

---

## 六、怎么判断走对了

不看功能数量，看三个行为指标：

1. **模型主动调用文献工具的频率** —— 阶段 B 是否真的成立了。
2. **批注留存率** —— 用户是否真的回来读自己标过的东西（3.3 的价值）。
3. **引用生成次数** —— 这是"从读到写"闭环的真实使用量，
   也是插件对用户研究流程介入深度的最终体现。
